# Production environment
# API is served via Nginx reverse proxy on same origin
VITE_API_BASE_URL=
VITE_API_V1_STR=/api/v1

# Whisper Transcription Service (local connection via same server)
VITE_WHISPER_API_URL=http://localhost:8091
